application:
  datasource:
    sqlite:
      url: jdbc:sqlite:datasync-user.db?journal_mode=WAL&busy_timeout=5000

  netty:
    server:
      port: 8443
      basePath: /path/to/local/sync/folder
    client:
      port: 8443
      host: your.server.host.or.ip

  jwt:
    secretkey: your_base64_encoded_jwt_secret_at_least_256bits
    freshTokenExpiration: 2592000000   # 30 days in ms
    accessTokenExpiration: 25920000000 # 300 days in ms
    token-name: Authorization
    uid: uid
    role: role

  mybatis:
    type-aliases-package: backend.model.entity
    configuration:
      log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
